import { relations } from 'drizzle-orm';
// import { users } from './users';

/**
 * Define your relationships here.
 * Modular relations separate the physical schema from the logic.
 */

/*
export const usersRelations = relations(users, ({ many }) => ({
  posts: many(posts),
}));
*/
